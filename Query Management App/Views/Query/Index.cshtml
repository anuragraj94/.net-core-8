@* @page *@
@using Query_Management_App.Models
@model List<Query>


@{
    //ViewData["Title"] = "Query Management";
}

<div class="container">
    <!-- Content here -->
<h2>Query Management</h2>



@if (Model != null && Model.Any()) // Check if Model is not null and contains data
{

        <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Requestor</th>
                <th>Status</th>
                <th>Created At</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var query in Model)
            {
                <tr>
                    <td>@query.Id</td>
                    <td>@query.Requestor</td>
                    <td>@query.Status</td>
                    <td>@query.CreatedAt.ToString("g")</td>
                    <td>
                            <a class="btn btn-primary btn-sm" href="/Query/Perform/@query.Id"  asp-controller="Query" asp-action="Perform" asp-route-id="@query.Id">Perform</a>
                            <a class="btn btn-success btn-sm" href="/Query/Resolve/@query.Id" asp-controller="Query" asp-action="Resolve" asp-route-id="@query.Id">Resolve</a>
                            <a class="btn btn-warning btn-sm" href="/Query/OnHold/@query.Id" asp-controller="Query" asp-action="OnHold" asp-route-id="@query.Id">On Hold</a>
                            <a class="btn btn-info btn-sm" href="/Query/Cancel/@query.Id" asp-page="./Cancel" asp-controller="Query" asp-action="Cancel" asp-route-id="@query.Id">Cancel</a>
                            <a class="btn btn-danger btn-sm" href="/Query/Delete/@query.Id" asp-page="./Cancel" asp-controller="Query" asp-action="Delete" asp-route-id="@query.Id">Delete</a>

                    </td>
                </tr>
            }
        </tbody>
    </table>

}
else
{
    <p>No queries found.</p> <!-- This message will show if there are no queries -->
}



    <a class="btn btn-primary" href="/Query/Raise" asp-controller="Query" asp-action="Raise">Raise New Query</a>




</div>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>